// Member Directory — Public Page
// Server Component wrapper. Data wiring (live BusinessListings) comes in a later step.

import DirectoryUI from '@/components/public/DirectoryUI';

export default function DirectoryPage() {
  return <DirectoryUI />;
}
